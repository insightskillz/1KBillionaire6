
pip install -r requirements.txt


Use `rasa train` to train a model.


rasa run actions
```

docker run -p 8000:8000 rasa/duckling

```
rasa shell --debug

## Rasa X Deployment
## Action Server Image

